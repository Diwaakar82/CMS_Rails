<h2>Edit Category</h2>

<%= form_with(model: @category, local: true) do |form| %>
    <p>
        <%= form.label :title %><br>
        <%= form.text_field :title %>
    </p>

    <p>
        <%= form.submit %>
    </p>
<% end %>